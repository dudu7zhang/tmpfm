"""Compare-model-representation flow extracted from training_model_builder."""


def maybe_compare_model_representation(cfg, logger, datamodule, load_plmodel_checkpoint):
    # compare_model_representation is fixed to the default disabled behavior.
    """Execute `maybe_compare_model_representation` and return values used by downstream logic."""
    return
