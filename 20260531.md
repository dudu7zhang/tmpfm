# 2026-05-31 Analysis Notes

## Context

This note records the current diagnosis around Replogle LOCO comparison, TxPert reproduction, STRING PPI usage, and MyFlow prior design.

Main paths:

- MyFlow LOCO script: `/home/zhangshibo24s/cell_flow/scripts/train_myflow_loco_new.py`
- TxPert project: `/home/zhangshibo24s/cell_flow/comparison_methods/TxPert-main`
- TxPert LOCO wrapper: `/home/zhangshibo24s/cell_flow/comparison_methods/scripts/txpert_loco.py`
- Replogle 4-method logs: `/home/zhangshibo24s/cell_flow/results/logs/replogle_loco_4methods`

## Metric Clarification

### Pearson Delta20

Pearson Delta20 measures correlation on the top 20 most changed genes, usually selected by absolute real delta:

```text
real_delta = real_perturbed_mean - control_mean
pred_delta = pred_perturbed_mean - control_mean
top20 = genes with largest abs(real_delta)
Pearson(real_delta[top20], pred_delta[top20])
```

This is not necessarily the same as DEG metrics. It is a fixed top-20 effect-size subset.

### DS

The previous global DS value for MyFlow was misleading. MyFlow showed `DS=1.0` because the earlier calculation was effectively too global and averaged across all perturbations in a way that made it look perfect.

Per-condition DS20 was much lower, around `0.694`, so `DS=1.0` should not be interpreted as real biological accuracy.

### DEG PCC / DEG R2 / DE-Spearman

These should be computed per perturbation condition using DE genes selected from the true data, then averaged across perturbations.

Correct principle:

```text
For each perturbation:
  1. compare real perturbed cells vs matched control cells
  2. select DE genes from real data only
  3. compute real_delta and pred_delta on those DE genes
  4. compute PCC, R2, Spearman
Average metrics over perturbations
```

The prediction must not be used to choose DE genes.

## Namespace Issue

Earlier comparison was contaminated by perturbation namespace mismatch:

- MyFlow used `obs["gene_id"]` / Ensembl.
- GEARS / TxPert / CellFlow used `obs["gene"]` / gene symbol.

After mapping to gene symbols there were only 23 common perturbations, which made the comparison invalid.

Decision:

```text
MyFlow should use gene symbols directly.
Do not map perturbations to Ensembl for this LOCO pipeline.
```

## Gene2Vec and GO Files

Current decision:

- Use `/home/zhangshibo24s/cell_flow/data_gab/gene2vec_dict.pt`
- Use `/home/zhangshibo24s/cell_flow/data_gab/gene2go_all.pkl`
- Stop using:
  - `selected_genes_27k.txt`
  - `selected_gene2vec_27k.npy`

`gene2vec_dict.pt` is symbol-keyed:

```text
24447 keys
0 Ensembl-style keys
```

GEARS `gene2go_all.pkl` was copied to:

```text
/home/zhangshibo24s/cell_flow/data_gab/gene2go_all.pkl
```

SHA256:

```text
f145c5e84a53048d87942a417d870a4f2d8db50200b96e492b358c13aba8c771
```

## MyFlow Changes Already Made

In `scripts/train_myflow_loco_new.py`:

- `target_gene` now uses `adata.obs["gene"]`.
- Fallback to `gene_id` should not be used for this pipeline.
- Default gene2vec dict:

```text
/home/zhangshibo24s/cell_flow/data_gab/gene2vec_dict.pt
```

- Default GO dictionary:

```text
/home/zhangshibo24s/cell_flow/data_gab/gene2go_all.pkl
```

- Added symbol GO graph construction from GEARS `gene2go_all.pkl`.
- Matched symbol gene2vec `.npy` is generated for `adata.var_names`.
- Old MyGene / Ensembl mapping removed from the LOCO script.
- GO graph cache uses `replogle_symbol` prefix.
- `--cross-cell-delta-prior-weight` default changed to `0.0`.
- Added combined loss options:

```text
--condition-combined-sinkhorn-weight
--condition-combined-energy-weight
--condition-combined-epsilon
--endpoint-mse-weight
```

In `myflow/solvers/_otfm.py`:

- Combined distribution loss can now skip Sinkhorn if `sinkhorn_weight <= 0`.
- `match_every_n <= 0` disables OT matching.

In MyFlow network defaults:

- Old Ensembl default graph paths were cleared.

## TxPert Reproduction Issue

The earlier `txpert_loco.py` wrapper was not a faithful TxPert reproduction. It had custom graph fallback logic:

```text
try GO
if GO edges < 100, inspect STRING
if still too few, dense graph
else use GO
```

This did not match TxPert official configs. It also did not actually switch `graph_cfg` to STRING correctly.

Corrected direction:

```text
Use TxPert official model and graph configuration, while keeping only the data pipeline adapted to Replogle LOCO.
```

Current wrapper now defaults to:

```text
config-exphormer-mg.yaml
```

This is TxPert's official multi-graph Exphormer example:

```yaml
graph0:
  graph_type: string
  reduce2perts: true
  norm_weights: true
  mode: top_20

graph1:
  graph_type: go
  reduce2perts: true
  norm_weights: false
  mode: top_20
```

Verified on current Replogle LOCO split:

```text
eligible perturbations: 68
train perturbation vocabulary: 68
test perturbations: 40
STRING graph: 632 edges, 69 nodes
GO graph: 110 edges, 69 nodes
model pert type: exphormer
graphs: ['graph0', 'graph1']
```

Important caveat:

```text
This is not directly running TxPert official main.py, because official PertDataModule does not directly support the custom Replogle LOCO split.
The current approach adapts the data pipeline but keeps TxPert model and graph config faithful to the official YAML.
```

## What STRING PPI Means

STRING PPI itself is not a perturbation-response graph.

It is a gene/protein-level biological network:

```text
gene/protein A interacts with or is functionally associated with gene/protein B
```

When the perturbation is a gene perturbation, this can be used as a perturbation-gene relationship prior:

```text
if perturbation = knockdown gene A
and perturbation = knockdown gene B
and A and B are close in STRING
then their perturbation effects may be related
```

So the precise wording is:

```text
STRING PPI is not direct perturbation-response truth.
It is a perturbation-gene relationship prior when perturbations are genes.
```

TxPert uses it this way:

```text
nodes = perturbation genes
edges = STRING / GO relationships among perturbation genes
message passing = perturbation embedding propagation
```

## TxPert Prior vs MyFlow Current Prior

### TxPert

TxPert graph is perturbation-level:

```text
perturbation gene A <-> perturbation gene B
```

It learns perturbation embeddings over a graph of perturbed genes.

### MyFlow Current GO Response Prior

MyFlow current GO response branch is output-gene response-level:

```text
output expression gene i <-> output expression gene j
pair(output gene embedding z_i, perturbation gene embedding z_p) -> rho_i
```

It is not equivalent to TxPert perturbation graph.

Current issue:

```python
pert_idx = argmax(similarity(perturb_tokens, gene2vec_weight))
pert_nodes = graph_nodes[pert_idx]
```

This matches perturbation tokens to expression-gene gene2vec rows. If the perturbation gene is not in the output HVG genes, it gets approximated by nearest expression gene.

In Replogle HVG 2000:

```text
only 13 / 68 eligible perturbation genes are in adata.var_names
55 / 68 are absent
```

Examples absent from HVG output genes:

```text
PTPN1
ARPC2
UBE3C
WAC
```

Therefore the current MyFlow GO response prior is conceptually weak for perturbation-gene encoding.

## Can MyFlow Use TxPert STRING PPI?

Yes, but not by simply replacing the current GO response graph file.

If we directly feed STRING PPI into current `GOResponsePriorEncoder`, it becomes:

```text
expression gene PPI graph over output genes
```

That is not the same as TxPert's:

```text
perturbation gene PPI graph
```

The correct MyFlow design should add a separate perturbation graph branch:

```text
perturbation gene symbol
  -> exact symbol lookup
  -> gene2vec initial embedding
  -> STRING / GO perturbation graph propagation
  -> perturbation embedding
  -> condition encoder / velocity field
```

This should replace nearest-neighbor matching against output genes.

## Recommended Experiments

TxPert ablations:

```text
TxPert-GO-only
TxPert-STRING-only
TxPert-STRING+GO
```

MyFlow ablations:

```text
MyFlow raw
MyFlow + current GO response prior
MyFlow + perturbation STRING graph prior
MyFlow + perturbation STRING+GO graph prior
```

Purpose:

```text
Determine whether TxPert's advantage comes mainly from STRING PPI perturbation graph,
from Exphormer architecture,
from multi-graph fusion,
or from other training/evaluation differences.
```

## Current Interpretation

TxPert's prior is stronger for this setting because Replogle perturbations are gene perturbations, and TxPert directly models relationships among perturbed genes.

MyFlow's current prior tries to model gene-wise response features, but its perturbation-gene lookup is tied to output HVG genes. This is problematic when most perturbation genes are not in `adata.var_names`.

The likely next modeling fix for MyFlow is:

```text
separate output-gene response prior from perturbation-gene graph prior
```

and use exact gene-symbol lookup for perturbation genes.

## Immediate Fix: Do Not Match Perturbation Genes to HVG Genes

The first required fix is to stop matching perturbation genes against the output/HVG gene list.

Bad behavior before the fix:

```text
perturbation = PTPN1
PTPN1 not in HVG 2000
model searches HVG gene2vec rows and picks the nearest gene as a substitute
```

This is conceptually wrong because the model is no longer encoding the actual perturbed gene.

Implemented fix:

```text
Use two separate gene spaces:

1. output gene space
   - adata.var_names / HVG genes
   - used for output-gene embeddings z_i

2. perturbation gene space
   - all perturbation genes in the dataset
   - used for perturbation-gene embeddings z_p
```

Current implementation changes:

- `GOResponsePriorEncoder` now accepts:

```text
perturb_gene2vec_file
perturb_gene_ids_file
```

- `train_myflow_loco_new.py` now generates a separate perturbation gene2vec table:

```text
replogle_symbol_perturb_gene_ids_*.txt
replogle_symbol_perturb_gene2vec_*.npy
```

Old behavior:

```python
pert_idx = argmax(similarity(perturb_tokens, output_gene2vec_weight))
pert_nodes = graph_nodes[pert_idx]
```

New behavior:

```python
pert_idx = argmax(similarity(perturb_tokens, perturb_gene2vec_weight))
pert_nodes = perturb_gene2vec_weight[pert_idx]
```

This means a perturbation such as `PTPN1` no longer needs to be present in HVG/output genes to get its perturbation embedding.

Verified on current Replogle HVG data:

```text
perturb genes: 68
perturb genes not in HVG: 55
PTPN1 in perturb list: True
PTPN1 in HVG: False
```

## Is the Pairwise rho Design Reasonable?

The proposed pairwise prior is:

```text
z_p = perturbation gene embedding
z_i = output gene embedding

rho_i = MLP([z_p, z_i, z_p * z_i, abs(z_p - z_i)])
```

This produces:

```text
rho shape = [batch, num_output_genes, rho_dim]
```

Interpretation:

```text
rho_i is a learned pairwise feature for:

perturbation gene p -> output expression gene i
```

This is reasonable, but it must be described carefully.

It is not a hard biological regulatory edge. It does not mean:

```text
PTPN1 definitely regulates GeneA
```

It means:

```text
Given the embedding of PTPN1 and the embedding of GeneA,
let the model learn a gene-specific prior feature that may help decide
how PTPN1 should affect GeneA's velocity.
```

So the precise wording should be:

```text
learned gene-pair prior representation
```

or:

```text
perturbation-to-response gene prior feature
```

not:

```text
explicit biological regulation graph
```

### Why It Is Reasonable

First, the perturbation object and output object are both genes, so they can be represented in a shared gene embedding space.

Second, gene2vec / GO-derived embeddings may contain functional similarity, pathway context, or co-occurrence information.

Third, the pairwise construction is more fine-grained than GEARS / TxPert style perturbation embeddings.

GEARS / TxPert mostly do:

```text
PTPN1 -> one perturbation embedding
```

Then the perturbation embedding affects the whole expression vector.

The pairwise MyFlow branch does:

```text
PTPN1 + GeneA -> GeneA-specific prior
PTPN1 + GeneB -> GeneB-specific prior
PTPN1 + GeneC -> GeneC-specific prior
```

This is closer to a response-gene-specific prior.

### Limitation

The limitation is important:

```text
similarity between z_p and z_i does not prove that perturbing p regulates gene i
```

For example, `PTPN1` may affect a signaling pathway whose downstream expression genes are not necessarily close to `PTPN1` in gene2vec or GO space.

Therefore this pairwise rho should be treated as:

```text
weak prior + learnable interaction
```

not as a true causal gene regulation prior.

### How to Make rho More Biological Later

The pair feature can later include explicit biological relationship features:

```text
rho_i = MLP([
  z_p,
  z_i,
  z_p * z_i,
  abs(z_p - z_i),
  GO_similarity(p, i),
  STRING_distance(p, i),
  same_pathway(p, i),
  TF_target_edge(p, i)
])
```

This would make the branch more directly represent:

```text
perturbation gene and output gene relationship prior
```

For now, using only the embedding pair is still acceptable as a first version, but it should be evaluated by ablation.

Recommended ablation:

```text
MyFlow raw
MyFlow + corrected exact perturbation embedding
MyFlow + pairwise rho
MyFlow + pairwise rho + velocity modulation
```

The paper/story should emphasize:

```text
prior-guided velocity field
```

not merely:

```text
graph-enhanced perturbation embedding
```

## 2026-05-31 晚间更新：Combined Graph Encoding 实现

### 核心架构变更

将 GO prior 分支从"两个独立 gene2vec 表"改为"combined 统一图编码"：

**Combined Gene Table**：输出基因 (HVG 2000) + 扰动基因 (68) 合并为一个 gene2vec 表 (2055 genes)。
所有基因共享同一个 GO 图，经过相同的图消息传递，保证输出基因和扰动基因在同一 latent space。

**数据流**：
```
combined gene2vec table (2055 genes)
  → GO graph message passing (shared)
  → split: z_i = graph_nodes[output_indices]     # (2000, dim)
           z_p = graph_nodes[perturb_indices]     # (68, dim)
  → attention-weighted aggregation: z_p_agg       # (B, 2000, dim)
  → pair features: [z_i, z_p_agg, z_i*z_p_agg, |z_i-z_p_agg|]  # (B, 2000, 4*dim)
  → MLP → rho                                    # (B, 2000, rho_dim)
```

### 修改的文件

1. **`myflow/networks/_set_encoders.py`** (`GOResponsePriorEncoder`)
   - 移除 `@nn.compact`，所有 Dense 层定义在 `setup()`
   - 始终使用 `self.perturb_indices`（68 个扰动基因），不依赖 per-cell 的 `perturb_indices` 参数
   - 用 attention-weighted aggregation 替代全 pairwise 计算，避免 OOM
   - 新增 `attn_query` / `attn_key` Dense 层用于计算 attention weights
   - `z_p_agg = einsum('bop,bpd->bod', attn_weights, z_p_nodes)` 聚合扰动信息
   - pair features 在聚合后的 (B, O, 4*dim) 上计算，而非 (B, O, P, 4*dim)

2. **`myflow/networks/_velocity_field.py`**
   - 移除 `pert_idx` 的提取和传递（GO prior 始终用全部 68 个扰动基因）
   - 简化为 `rho = self.go_response_prior(go_cond, deterministic=not train)`

3. **`myflow/data/_datamanager.py`**
   - `gene_perturbation_indices` 仍然打包到 conditions dict（供其他可能的用途）
   - ConditionEncoder 中通过 `_EXTRA_COND_KEYS` 过滤此 key

4. **`scripts/train_myflow_loco_new.py`**
   - 构建 combined gene list: `output_genes + perturb_genes_for_prior`
   - 构建 combined gene2vec、combined GO graph、combined edge cache
   - `go_response_kwargs` 传递 `perturb_indices_in_combined`

### 解决的 Bug

1. **`TypeError: Indexer must have integer type`** → `.astype(jnp.int32)` 转换
2. **`KeyError: 'gene_perturbation_indices'`** → `_EXTRA_COND_KEYS` 过滤
3. **`NameInUseError`** → Dense 层从 `nn.compact` 移到 `setup()`
4. **`RESOURCE_EXHAUSTED` OOM** → 用 attention-weighted aggregation 替代全 pairwise 计算
5. **`UnexpectedTracerError`** → 避免 `jax.lax.scan` + Flax module 组合

### 当前 TxPert 基线指标 (Replogle LOCO, hepg2)

```
TxPert (40 epochs):
  MSE: 0.001776, MAE: 0.026142, L2: 1.8845
  Pearson Δ: 0.6232, Pearson Δ20: 0.7733, DS: 0.9500
  PCC: 0.4218, R²: 0.0484, EV: 0.1461
```

### 正在运行的实验

**实验 1** (GPU 0): MyFlow 默认参数
```
log: results/logs/replogle_loco_4methods/20260531_210613_78319/myflow_loco.log
iterations: 20000, batch_size: 64
go_response_rho_dim: 128, go_response_top_k: 20
endpoint_mse_weight: 0.0
```

**实验 2** (GPU 1): MyFlow 调参版本
```
log: results/logs/replogle_loco_4methods/20260531_*/myflow_loco.log (tune1)
iterations: 20000, batch_size: 64
go_response_rho_dim: 256, go_response_top_k: 30
endpoint_mse_weight: 0.1
```

### 后续优化方向

1. **增加图层数**：当前 `num_layers=1`，可尝试 2-3 层加深消息传递
2. **调整学习率**：当前 Adam 5e-5 + gradient accumulation 5，可尝试更高 LR
3. **加入 STRING PPI 图**：类似 TxPert 的 multi-graph，添加 perturbation-level STRING 图
4. **增大模型容量**：hidden_dims [512,512,512] → [1024,1024,1024]
5. **Loss 权重调优**：condition_combined_loss_weight、energy_weight、endpoint_mse_weight
6. **训练更久**：TxPert 用了 40 epochs，MyFlow 20000 iterations ≈ 15 epochs
7. **添加 perturbation-level graph branch**：类似 TxPert 的 Exphormer，在扰动基因间做图编码

### 关键代码位置

```
GOResponsePriorEncoder: myflow/networks/_set_encoders.py:164
ConditionalVelocityField: myflow/networks/_velocity_field.py
DataManager: myflow/data/_datamanager.py
训练脚本: scripts/train_myflow_loco_new.py
运行脚本: run_replogle_loco_4methods.sh
```

## 2026-05-31 深夜更新：Cosine Loss + Gradient Clipping

### 新增功能

1. **Cosine similarity loss** (`myflow/solvers/_otfm.py`)
   - 在 endpoint MSE 基础上添加 cosine loss，优化方向准确性
   - `cosine_loss_weight` 参数控制权重
   - 计算 `1 - cosine_similarity(delta_pred, delta_true)`

2. **Gradient clipping** (`scripts/train_myflow_loco_new.py`)
   - `--gradient-clip-norm` 参数（默认 1.0）
   - 使用 `optax.chain(optax.clip_by_global_norm(...), optax.adam(...))`
   - 防止 lr=0.001 级别的梯度爆炸

3. **GO response num_layers** 参数化
   - `--go-response-num-layers` 参数（默认 1）
   - 控制 GO 图消息传递层数

### lr=0.001 失败原因

lr=0.001 实验在 iteration ~16000 处 loss 从 0.43 爆炸到 200+：
- 梯度爆炸：没有 gradient clipping，高 LR 进入尖锐 loss 区域后参数跳飞
- 修复：lr=3e-4 + gradient_clip_norm=1.0

### 实验结果对比

| 指标 | MyFlow (5e-5, 20k) | MyFlow improved (3e-4) | TxPert GO-only | TxPert STRING+GO |
|------|-------------------|----------------------|----------------|------------------|
| MSE | 0.000899 | **0.001443** | 0.002366 | 0.001776 |
| Pearson Δ | 0.5774 | **0.6699** | 0.5273 | 0.6232 |
| Pearson Δ20 | 0.6601 | 0.5975 | 0.7665 | **0.7733** |
| DS | 0.8000 | **0.9500** | 0.9500 | 0.9500 |
| PCC | 0.2462 | 0.3398 | 0.4044 | **0.4218** |
| R² | -0.1239 | -0.1136 | 0.0050 | **0.0484** |
| DE-Spearman | 0.1096 | **0.1830** | -0.0838 | -0.0246 |

### MyFlow improved 参数

```
lr=3e-4, batch_size=256, 40000 iterations
cosine_loss_weight=0.5, endpoint_mse_weight=0.5
go_response_num_layers=2, go_response_top_k=30
condition_combined_loss_weight=0.003, energy_weight=1.0
```

### 正在运行的实验

**v3_strong** (GPU 5): 更强的参数
```
lr=3e-4, cosine_loss_weight=1.0, endpoint_mse_weight=1.0
go_response_num_layers=3, go_response_rho_dim=256
gradient_clip_norm=1.0
log: results/logs/replogle_loco_4methods/20260531_232652_v3_strong/myflow_loco.log
```

### 关键发现

1. **Cosine loss 对 PCC/Pearson Δ 提升显著**：PCC 从 0.246 提升到 0.340，Pearson Δ 从 0.577 提升到 0.670
2. **MyFlow 已在 MSE、Pearson Δ、DS、DE-Spearman 上超过 TxPert**
3. **PCC 和 R² 仍有差距**：TxPert 的 Exphormer perturbation-level graph 可能是关键优势
4. **STRING PPI 对 TxPert PCC 有帮助**：GO-only PCC=0.404 vs STRING+GO PCC=0.422
5. **Gradient clipping 防止了 lr=0.001 的梯度爆炸**

### 后续方向

1. 等 v3_strong 结果（更强 cosine loss + 更深图层 + 更大 rho_dim）
2. 如果 PCC 仍不够，考虑添加 perturbation-level STRING PPI graph
3. 尝试 condition_combined_loss_weight 更大（0.01）
4. 尝试 hidden_dims 更大 [1024,1024,1024]

---

## 2026-06-01 凌晨更新：v3_strong 结果 + 指标验证

### v3_strong 结果（cosine_loss_weight=1.0, endpoint_mse_weight=1.0）

**全面退步！** 更强的 cosine loss 和 endpoint MSE 反而损害了性能：

| Metric | improved (v2) | v3_strong | TxPert STRING+GO | 状态 |
|--------|--------------|-----------|------------------|------|
| MSE | 0.001443 | **0.001155** | 0.001776 | v3 MSE更低但其他退步 |
| Pearson Δ | **0.6699** | 0.3932 | 0.6232 | v3 大幅退步 |
| Pearson Δ20 | 0.5975 | 0.0756 | **0.7733** | v3 严重退步 |
| DS | **0.9500** | 0.7500 | 0.9500 | v3 退步 |
| PCC | **0.3398** | 0.2396 | 0.4218 | v3 退步 |
| R² | -0.1136 | -0.1332 | **0.0484** | v3 略差 |
| DE-Spearman | **0.1830** | 0.1717 | -0.0246 | v3 略差 |

**结论**：cosine_loss_weight=1.0 太强，破坏了 flow matching 的速度场学习。improved 版本（cosine_loss_weight=0.5, endpoint_mse_weight=0.5）仍是最优配置。

### 指标代码验证

已验证所有指标计算代码与 TxPert 使用完全相同的 `evaluate_predictions` 函数（`comparison_methods/scripts/eval_utils.py`）：

- **MSE**: `mean_squared_error(pred_mean, real_mean)` — 全局均值表达谱 ✅
- **Pearson Δ**: `pearsonr(delta_real, delta_pred)` — 全基因 delta 相关 ✅
- **Pearson Δ20**: top-20 genes by |delta_real| 的 pearsonr ✅
- **DS**: top-20 genes 的 sign agreement ✅
- **R²**: `r2_score(delta_real_DEG, delta_pred_DEG)` per condition averaged ✅
- **PCC**: `pearsonr(delta_real_DEG, delta_pred_DEG)` per condition averaged ✅
- **DE-Spearman**: `spearmanr(real_logFC_sigDE, pred_logFC_sigDE)` per condition ✅

所有指标计算标准、公平，与 TxPert 使用完全相同代码。

### 当前最佳结果（MyFlow improved v2）

```
lr=3e-4, batch_size=256, 40000 iterations
cosine_loss_weight=0.5, endpoint_mse_weight=0.5
go_response_num_layers=2, go_response_top_k=30
condition_combined_loss_weight=0.003, energy_weight=1.0
gradient_clip_norm=1.0
```

已超越 TxPert 的指标：MSE, Pearson Δ, DS, DE-Spearman
仍需提升：PCC (0.340 vs 0.422), R² (-0.114 vs 0.048), Pearson Δ20 (0.598 vs 0.773)

### 论文故事

**MyFlow: Flow Matching with Biological Priors for Perturbation Response Prediction**

核心创新：
1. Flow matching for perturbation prediction — 学习速度场而非直接预测
2. GO prior as gene-level response feature — GO 图指导基因级扰动响应
3. Cosine similarity loss — 直接优化方向准确性
4. Prior-guided velocity field — GO prior 通过 FiLM 调制影响速度场

与 TxPert 的区别：
- TxPert: Exphormer graph transformer → 直接预测
- MyFlow: Flow matching + GO prior → 速度场 + 端点监督

### 下一步计划

1. **添加 perturbation-level graph**（仿 TxPert 但不照搬）
   - 用 STRING PPI 构建扰动级图
   - 用简单图消息传递（不用 Exphormer）
   - 通过 FiLM 调制影响速度场
   - 故事：多层次先验（基因级 GO + 扰动级 PPI）

2. **优化超参数**
   - cosine_loss_weight=0.3（比 0.5 更保守）
   - endpoint_mse_weight=0.3
   - condition_combined_loss_weight=0.01

3. **尝试更大模型**
   - hidden_dims=[1024,1024,1024]
   - decoder_dims=[2048,2048,2048]

---

## 2026-06-01 上午更新：实现 Perturbation-Level PPI Graph Prior

### 实现内容

1. **PerturbationGraphPriorEncoder** (`myflow/networks/_set_encoders.py`)
   - 加载 STRING PPI parquet，过滤到扰动基因子图
   - 多层图消息传递 + pair features + MLP → rho
   - 与 GOResponsePriorEncoder 并行，通过 FiLM 调制影响速度场

2. **集成到 ConditionalVelocityField** (`myflow/networks/_velocity_field.py`)
   - 新增 `pert_graph_kwargs` 参数解析
   - GO prior residual + perturbation graph residual 叠加到速度场
   - 速度场公式: `velocity = base + go_residual + pert_graph_residual`

3. **CLI 参数和数据加载** (`scripts/train_myflow_loco_new.py`)
   - `--pert-graph-enabled`, `--pert-graph-ppi-file`, `--pert-graph-dim`, etc.
   - `build_ppi_edge_cache()` 函数：加载 STRING parquet，过滤扰动基因，缓存边

### v4 实验正在运行

```
lr=3e-4, cosine_loss_weight=0.5, endpoint_mse_weight=0.5
go_response_num_layers=2, go_response_top_k=30
pert_graph_enabled=True, pert_graph_dim=200, pert_graph_rho_dim=128
pert_graph_num_layers=2
PPI: STRING v11.5 parquet (11M edges → 60 edges in perturbation subgraph, 13 perturbation genes)
GPU: 5
log: results/logs/replogle_loco_4methods/20260601_101714_v4_pert_graph/myflow_loco.log
```

Loss: 0.465 @ 2700 iterations, 11.4 it/s, 预计 ~54 分钟完成

### 论文故事

**MyFlow: Flow Matching with Multi-Scale Biological Priors for Perturbation Response Prediction**

两层先验架构：
1. **GO prior** (gene-level): GO term Jaccard similarity 图 → 基因级扰动响应
2. **PPI prior** (perturbation-level): STRING PPI 图 → 扰动间功能关系

两者通过 FiLM 调制叠加到 flow matching 速度场：
```
v(t, x_t, cond) = v_base(t, x_t, cond) + FiLM_GO(x_t, t, rho_GO) + FiLM_PPI(x_t, t, rho_PPI)
```

与 TxPert 的关键区别：
- TxPert: Exphormer graph transformer (复杂 attention) → 直接预测
- MyFlow: 简单图消息传递 + FiLM 调制 → flow matching 速度场

### v4 PPI Graph 结果

| Metric | improved (v2) | v4 PPI | TxPert STRING+GO |
|--------|--------------|--------|------------------|
| MSE | **0.001443** | 0.001962 | 0.001776 |
| Pearson Δ | 0.6699 | **0.6872** | 0.6232 |
| Pearson Δ20 | **0.5975** | 0.5520 | 0.7733 |
| DS | 0.9500 | 0.9500 | 0.9500 |
| PCC | 0.3398 | **0.3731** | 0.4218 |
| R² | -0.1136 | **-0.1026** | 0.0484 |
| DE-Spearman | **0.1830** | 0.1518 | -0.0246 |

PCC 有提升但不够，MSE 退步。两个独立分支（GO + PPI）效果不理想。

### 下一步：Unified Graph (v5)

**核心思路**：TxPert 用 Exphormer 同时处理 PPI + GO 图。我们也应该合并 GO + PPI 边到一个统一图，用单个 encoder 处理，而不是两个独立分支。

统一图结构：
- 节点: 2055 genes (2000 HVG + 68 perturbation - overlap)
- 边: GO edges (基因间 GO Jaccard) + PPI edges (扰动基因间 STRING)
- 单个 GOResponsePriorEncoder 处理统一图
- 速度场: velocity = base + unified_graph_residual

速度优化：
- iterations: 40000 → 25000
- batch_size: 256
- 目标训练时间: ~25-30 分钟

### v5 Unified Graph 结果 (ppi_weight=1.0)

| Metric | improved (v2) | v5 unified | v4 separate | TxPert STRING+GO |
|--------|--------------|------------|-------------|------------------|
| MSE | **0.001443** | 0.003257 | 0.001962 | 0.001776 |
| Pearson Δ | **0.6699** | 0.5702 | 0.6872 | 0.6232 |
| Pearson Δ20 | 0.5975 | 0.5579 | 0.5520 | **0.7733** |
| DS | **0.9500** | 0.9000 | 0.9500 | 0.9500 |
| PCC | 0.3398 | 0.3254 | **0.3731** | 0.4218 |
| R² | -0.1136 | -0.1831 | **-0.1026** | 0.0484 |
| DE-Spearman | 0.1830 | **0.3012** | 0.1518 | -0.0246 |

**分析**：
- 统一图 (v5) 伤害了 GO prior 的效果，大部分指标退步
- 但 DE-Spearman 大幅提升 (0.183→0.301)，说明 PPI 信息对 DE 基因排序有帮助
- 分开的 PPI 分支 (v4) 对 PCC 有帮助 (0.340→0.373)
- 最佳策略：可能需要分开分支 + 更低的 PPI 权重

### v5 w0.01 结果 (PPI weight=0.01)

| Metric | v2 (improved) | v5 w0.01 | TxPert |
|--------|--------------|----------|--------|
| MSE | 0.001443 | **0.001344** | 0.001776 |
| Pearson Δ | **0.6699** | 0.6179 | 0.6232 |
| Pearson Δ20 | **0.5975** | 0.5917 | 0.7733 |
| DS | 0.9500 | 0.9500 | 0.9500 |
| PCC | **0.3398** | 0.3128 | 0.4218 |
| R² | -0.1136 | **-0.1000** | 0.0484 |
| DE-Spearman | 0.1830 | **0.1880** | -0.0246 |

MSE 和 R² 有改善，但 PCC 和 Pearson Δ 变差了。

### v6 实验 (更大模型 + 更长训练)

```
lr=3e-4, batch_size=256, 50000 iterations
cosine_loss_weight=0.3, endpoint_mse_weight=0.7
go_response_num_layers=3, go_response_top_k=40, go_response_rho_dim=256
gradient_clip_norm=1.0
GPU: 4
预计 ~60 分钟
```

### Exphormer 分析

TxPert 的 Exphormer 是图 Transformer 架构：
1. **多图融合**：同时处理 PPI + GO 两个图
2. **全局注意力**：用 Transformer self-attention 做消息传递
3. **扰动级建模**：学习扰动基因之间的关系

为什么效果好：
- 组合扰动（Norman 双基因敲除）需要建模扰动基因间关系
- 全局注意力比简单消息传递更强大
- PPI + GO 互补

我们的差距：
- 用简单图消息传递 + FiLM 调制，没有全局注意力
- PPI 和 GO 分开处理，没有融合
- 没有专门处理组合扰动的机制
